# Production dependencies for Python 3.13.
# Versions already present in the working Python 3.13 environment are kept
# where possible. Missing runtime packages are pinned to Python 3.13-compatible
# releases from PyPI. Test-only dependencies are intentionally excluded.

# ASGI API
fastapi==0.139.2
starlette==1.3.1
uvicorn==0.51.0
pydantic==2.12.5
python-multipart==0.0.32

# Configuration, HTTP, authentication, email, and payments
python-dotenv==1.0.0
httpx==0.28.1
PyJWT==2.10.1
pwdlib[argon2]==0.3.1
email-validator==2.3.0
fastapi-mail==1.6.5
python-alipay-sdk==3.4.0

# SQL databases and migrations
SQLAlchemy==2.0.43
alembic==1.18.5
aiomysql==0.3.2
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# Redis and RabbitMQ
redis==8.1.0
aio-pika==10.0.1

# LangChain, LangGraph, DeepSeek, Ollama, Chroma, and PostgreSQL checkpoints
langchain-core==1.4.0
langchain-community==0.4.2
langchain-deepseek==1.0.1
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langgraph==1.2.2
langgraph-checkpoint-postgres==3.1.2

# PDF generation and parsing
reportlab==5.0.0
pypdf==6.14.2
Pillow==12.3.0
